"""Measurement framework for the support-agent-token-benchmark project.

Modules:
    tokens   — token counting and 6-category decomposition (Step 2; the
               original Silicon Data five plus agent_intermediate for
               multi-turn loops — see METHODOLOGY §"Multi-turn extension")
    runner   — orchestration of full task runs across architectures (Step 3)
"""
