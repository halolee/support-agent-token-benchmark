# Environment
.env
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.pytest_cache/
.coverage
htmlcov/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Vector stores (per-architecture)
architectures/*/vector_store/
architectures_deferred/*/vector_store/

# BGE-M3 / HuggingFace model cache (if HF_HOME is set locally)
.cache/huggingface/

# Downloaded reference artifacts (fetch via Phase 1 Step 0 — see BUILD_PLAN)
# travel.sqlite is ~110MB (over GitHub's per-file limit); source URLs + hashes are recorded in notebooks/00_corpus_inventory.ipynb.
corpus/*.md
data/*.sqlite

# Measurement output (keep results/comparison.md but ignore raw JSON dumps)
measurement/results/*.json
measurement/results/raw_runs/

# Jupyter
.ipynb_checkpoints/

# Local notes
NOTES.md
TODO.md
scratch/
