[pytest]
markers =
    slow: marks tests that require downloaded models or other heavy resources (deselect with `-m 'not slow'`)
    requires_api_key: marks tests that call the real Anthropic API (auto-skipped if ANTHROPIC_API_KEY is not set)
testpaths = tests
