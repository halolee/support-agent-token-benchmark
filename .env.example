# Anthropic API key — required for agent inference and judge scoring
# Get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# No OPENAI_API_KEY required: embeddings use self-hosted BGE-M3 via
# sentence-transformers (see METHODOLOGY § "Model and configuration").
# No third-party embedding service is used by this project.
